load("@org_cloudabi_bazel_toolchains_cloudabi//:cc.bzl", "cc_binary_cloudabi")

cc_binary_cloudabi(
    name = "cloudabi-reexec",
    srcs = ["cloudabi-reexec.c"],
    visibility = ["//visibility:public"],
    deps = ["@org_cloudabi_argdata//:argdata"],
)
